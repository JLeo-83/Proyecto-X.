import { Canvas } from "@react-three/fiber"; // Importa Canvas
import { PerspectiveCamera } from "@react-three/drei"; // Importa PerspectiveCamera

const Hero = () => {
  return (
    <section className="min-h-screen w-full flex flex-col relative"></section>
  );
};

export default Hero;
